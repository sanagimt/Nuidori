<%= form_with model: post do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :body %>

  <% if post.id.nil? %>
    <%= f.submit '投稿' %>
  <% else %>
    <%= f.submit '更新' %>
  <% end %> 
  
<% end %>