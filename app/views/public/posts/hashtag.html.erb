<div class="container">
  <div class="row">
    <div class="col mt-4 ml-5">
      <h4>ハッシュタグ「#<%= @content %>」の投稿検索結果（<%= @records.total_count %>件）</h4>
    </div>
  </div>

  <div class="row mt-3 mb-3">
    <div class="col ml-5">
      <% if @records.any? %>
        <div class="d-flex flex-wrap mx-5">
          <%= render 'public/posts/index', posts: @records %>
        </div>
        <%= paginate @records %>
      <% else %>
        <p>該当する投稿は見つかりませんでした。</p>
      <% end %>
    </div>
  </div>
</div>