<div class="container">
  <div class="row">
    <div class="col mt-5 ml-5">
      <% if @model == 'post' %>
        <h3>「<%= @content %>」の投稿検索結果（<%= @content.blank? ? 0 : @records.count %>件）</h3>
      <% elsif @model == 'user' %>
        <h3>「<%= @content %>」のユーザー検索結果（<%= @content.blank? ? 0 : @records.count %>件）</h3>
      <% end %>
    </div>
  </div>

  <div class="row mt-3 mb-3">
    <div class="col ml-5">
      <% if @model == 'post' %>
        <% if @content.blank? %>
          <p>検索キーワードを入力してください。</p>
        <% elsif @records.any? %>
          <div class="d-flex flex-wrap mx-5">
            <%= render 'public/posts/index', posts: @records %>
            <%= paginate @records %>
          </div>
        <% else %>
          <p>該当する投稿は見つかりませんでした。</p>
        <% end %>

      <% elsif @model == 'user' %>
        <% if @content.blank? %>
          <p>検索キーワードを入力してください。</p>
        <% elsif @records.any? %>
          <%= render 'public/users/index', users: @records %>
          <%= paginate @records %>
        <% else %>
          <p>該当するユーザーは見つかりませんでした。</p>
        <% end %>
      <% end %>
    </div>
  </div>
</div>