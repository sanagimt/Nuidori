<% if admin_signed_in? %>
  <%= form_with url: admin_search_path, local: true, method: :get, html: { class: "cute-search-form" } do |f| %>
    <%= f.text_field :content, placeholder: "キーワード", class: "cute-input" %>
    <%= f.select :model, options_for_select({"ユーザー" => "user", "投稿" => "post"}), {}, class: "cute-select" %>
    <%= f.submit "🔍 検索", class: "cute-submit" %>
  <% end %>
<% elsif user_signed_in? %>
  <%= form_with url: search_path, local: true, method: :get, html: { class: "cute-search-form" } do |f| %>
    <%= f.text_field :content, placeholder: "キーワード", class: "cute-input" %>
    <%= f.select :model, options_for_select({"ユーザー" => "user", "投稿" => "post"}), {}, class: "cute-select" %>
    <%= f.submit "🔍 検索", class: "cute-submit" %>
  <% end %>
<% end %>