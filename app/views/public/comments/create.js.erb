{
  document.getElementById("comments").insertAdjacentHTML(
    "beforeend",
    "<%= j render partial: 'public/comments/post_comment', locals: { comment: @comment, post: @post } %>"
  );

  document.getElementById("comment_count").textContent = "<%= @post.comments.count %>";

  const noCommentMsg = document.getElementById("no-comment-msg");
  if (noCommentMsg) noCommentMsg.remove();

  document.getElementById("comment_textarea").value = "";
  document.getElementById("char_count").textContent = "0";
  document.getElementById("submit_comment").disabled = true;

  setupCommentFormListeners();
}