{
  const targetComment = document.getElementById("comment-<%= @comment.id %>");
  if (targetComment) targetComment.remove();

  document.getElementById("comment_count").textContent = "<%= @post.comments.count %>";

  const existingNoCommentMsg = document.getElementById("no-comment-msg");
  if (<%= @post.comments.count %> === 0 && !existingNoCommentMsg) {
    document.getElementById("comments-list").insertAdjacentHTML(
      "beforeend",
      "<p id='no-comment-msg' class='text-muted'>コメントはまだありません。</p>"
    );
  }
}